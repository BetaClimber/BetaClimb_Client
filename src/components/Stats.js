import React, { Component } from 'react';

export class Stats extends Component {
  render() {
    return(
      <p>This is the D3 page!</p>
      /*
        I want to display a total climbs number

        I want to add badges for achievement conditionals
          -Build the backend!
          Make a db request and get the total distance climbed
          Make a request to the db, look into fetch.
          - Create the switch statment
          - have a png to represent the award within the images folder
          - render the image once the page is visited and the award is achieved

      */
    );
  }
}
