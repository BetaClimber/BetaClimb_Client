import React, { Component } from 'react';

export class Logs extends Component {
  render() {
    return(
      <p>This is the logs page!</p>
      /*make a request from the backend and get a list of climbs
        - Build the backend first before anything
        - Make a join using an ORM from CJ's breakout!
      */
    );
  }
}
